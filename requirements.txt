# === GPU CONFIGURATION ===
# Forces pip to look for CUDA-optimized wheels first.
# This ensures you get 'torch-cu121' (GPU) instead of 'torch' (CPU).
--extra-index-url https://download.pytorch.org/whl/cu121

# --- CORE DL & GPU (RTX 4060 Optimized) ---
torch
torchvision
torchaudio

# --- NLP & TRANSFORMERS ---
# Pinned to 4.46.3 to support legacy models (.bin) and avoid CVE errors.
transformers==4.46.3
datasets
setfit
sentence-transformers
accelerate
einops

# --- DATA SCIENCE ---
numpy
pandas
scikit-learn
xgboost
matplotlib
seaborn

# --- UTILS & NOTEBOOK ---
ipykernel
tqdm